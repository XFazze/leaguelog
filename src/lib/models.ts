export type User = {
    id: string,
    accountId: string,
    puuid: string,
    username: string;
    profileIconId: number,
    revisionDate: number,
    summonerLevel: number,
    region: string;
    level: number;
    pfp_url: string;
    uuid: string;
}